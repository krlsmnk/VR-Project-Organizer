using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Car{

    public int identification, horsepower, valves;
    public double length, width, height, wheelbase, curbWeight, grossWeight, fuelCapacity, frontHead, frontHip, frontLeg, frontShoulder, rearHead, rearHip, rearLeg, rearShoulder, cargoCapacity, maxSpeed, drag;
	public string make, model, year, trim, description, carClass, url, bType, cylinders, valveTiming, driveType, transmission, engineType, fuelType, mpg;

}
