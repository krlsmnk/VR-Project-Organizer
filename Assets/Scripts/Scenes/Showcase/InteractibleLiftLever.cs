using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class InteractibleLiftLever : MonoBehaviour {

	private 

	// Use this for initialization
	void Start () {
		//lift initiates down
		
	}
	
	// Update is called once per frame
	void Update () {

		//if lift lever in max (forward) position, and lift is up, put lift down

		//if lift lever in min (backward) position, and lift is down, put lift up
		
	}
}
